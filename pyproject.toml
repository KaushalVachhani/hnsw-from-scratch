[project]
name = "hnsw-from-scratch"
version = "0.1.0"
description = "Learning nearest-neighbor search by building toward HNSW from scratch"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "matplotlib>=3.11.1",
    "numpy>=2.5.2",
    "pandas>=3.0.5",
    "pillow>=12.3.0",
]
